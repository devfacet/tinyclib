{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug tl_app",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/tests/bin/tl_app",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "CMake: Build"
    }
  ]
}
